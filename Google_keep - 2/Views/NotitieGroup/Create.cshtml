@model Google_keep___2.Models.NotitieGroups

@{
    ViewBag.Title = "Notitie group Create";
}

<h2>Create Note Group</h2>

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true);
    <div>
        <div class="editor-label">
            <b>Note Group</b>
        </div>
        <div class="editor-field">
            @Html.EditorFor(n => n.NotitieGroup)  
            @Html.ValidationMessageFor(n => n.NotitieGroup)
        </div>
        <input type="submit" value="Create" />|@Html.ActionLink("Back", "Create", "Home")
    </div>
}